# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=locket_db
DB_PORT=3306

# Flask Configuration
FLASK_HOST=0.0.0.0
FLASK_PORT=5000
FLASK_DEBUG=False

# Redis Configuration (Optional - falls back to in-memory cache if not available)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# Cache Configuration (in seconds)
CACHE_TTL_SHORT=60
CACHE_TTL_MEDIUM=300
CACHE_TTL_LONG=600

# JWT Configuration
JWT_SECRET=sua_chave_secreta_muito_segura_altere_isso_em_producao
JWT_EXPIRATION=3600

# Application Configuration
APP_ENV=production
LOG_LEVEL=error

# Paths
MODEL_PATH=models/delay_model.pkl
REPORTS_DIR=reports


